{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "target": "ESNext", // Compiles to latest ECMAScript version
    "module": "NodeNext", // Uses the latest ES module system
    "moduleResolution": "NodeNext", // Ensures Node.js-style module resolution
    "outDir": "dist",
    "lib": ["ESNext"] // Uses the latest ES library
  },
  "exclude": ["node_modules"],
  "include": ["src/**/*"]
}
